# Neural meta-classifier. Self-trains from rspamd's own confident decisions.
enabled = true;
train {
    max_trains = 1000;
    max_usages = 200;
    max_iterations = 25;
    learning_rate = 0.01;
    spam_score = 8;
    ham_score = -2;
}
symbol_spam = "NEURAL_SPAM";
symbol_ham = "NEURAL_HAM";
