# Cosmetic labels only. The filter ignores rspamd's action and decides
# from the raw score: accounts.yml `threshold` (Inbox → Junk) and
# `rescue_below` (provider Junk → Inbox). Changing the numbers below
# does not change those moves.
reject = 15;
add_header = 6;
greylist = 4;
