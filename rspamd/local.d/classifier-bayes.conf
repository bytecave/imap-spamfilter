# Per-recipient Bayes backed by Redis. autolearn explicitly disabled - we only
# learn from explicit user moves via the filter's /learnspam and /learnham calls.
backend = "redis";
new_schema = true;
autolearn = false;
users_enabled = true;
expire = 0;
min_tokens = 11;
min_learns = 200;
