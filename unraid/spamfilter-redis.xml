<?xml version="1.0"?>
<Container version="2">
  <Name>spamfilter-redis</Name>
  <Repository>redis:8-alpine</Repository>
  <Registry>https://hub.docker.com/_/redis/</Registry>
  <Network>spamnet</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/marcelverdult/imap-spamfilter/issues</Support>
  <Project>https://github.com/marcelverdult/imap-spamfilter</Project>
  <Overview>
    Redis persistence backend for the IMAP spam filter stack. Stores rspamd
    Bayes tokens, fuzzy hashes, and neural weights. AOF + RDB enabled so
    the corpus survives container restarts. Install on the spamnet network
    alongside spamfilter-rspamd, spamfilter-unbound, and spamfilter.
  </Overview>
  <Category>Productivity Network-Other</Category>
  <WebUI/>
  <TemplateURL>https://raw.githubusercontent.com/marcelverdult/imap-spamfilter/main/unraid/spamfilter-redis.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/marcelverdult/imap-spamfilter/main/assets/icon.png</Icon>
  <ExtraParams>--log-opt max-size=50m --log-opt max-file=5</ExtraParams>
  <PostArgs>redis-server /usr/local/etc/redis/redis.conf</PostArgs>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>Run unraid/bootstrap.sh first: it creates the `spamnet` network, the appdata layout, and renders the Redis server config (including the auth password) that this container starts from.</Requires>
  <Config Name="Data dir" Target="/data" Default="/mnt/user/appdata/spamfilter/redis" Mode="rw" Description="Redis AOF/RDB files. Critical to back up - losing this means retraining Bayes from zero." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/spamfilter/redis</Config>
  <Config Name="Server config dir" Target="/usr/local/etc/redis" Default="/mnt/user/appdata/spamfilter/redis-config" Mode="ro" Description="Directory holding the bootstrap-rendered redis.conf (persistence, memory cap, requirepass auth). Read-only. Run bootstrap.sh before installing." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/spamfilter/redis-config</Config>
</Container>
