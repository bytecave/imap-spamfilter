<?xml version="1.0"?>
<Container version="2">
  <Name>spamfilter</Name>
  <Repository>ghcr.io/marcelverdult/imap-spamfilter:latest</Repository>
  <Registry>https://ghcr.io/marcelverdult/imap-spamfilter</Registry>
  <Network>spamnet</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/marcelverdult/imap-spamfilter/issues</Support>
  <Project>https://github.com/marcelverdult/imap-spamfilter</Project>
  <Overview>
    IMAP spam filter for any IMAP/IDLE-capable mailbox.
    One thread per account, scores via the companion rspamd container,
    moves to Junk based on per-account rules, learns Bayes from your
    own Inbox&lt;-&gt;Junk moves. Never deletes mail (MOVE only).

    Per-account config (credentials, modes, threshold) lives in
    `/mnt/user/appdata/spamfilter/accounts.yml`. Edit that file by hand;
    container picks it up on restart.

    Requires the companion containers spamfilter-redis,
    spamfilter-rspamd, and spamfilter-unbound on the `spamnet` network.
  </Overview>
  <Category>Productivity Security Network:Other</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/marcelverdult/imap-spamfilter/main/unraid/spamfilter.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/marcelverdult/imap-spamfilter/main/assets/icon.png</Icon>
  <ExtraParams>--log-opt max-size=50m --log-opt max-file=5</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>Custom network `spamnet`. spamfilter-rspamd must be running on the same network. Create accounts.yml before first start (see accounts.yml.example in the repo).</Requires>

  <Config Name="accounts.yml" Target="/app/accounts.yml" Default="/mnt/user/appdata/spamfilter/accounts.yml" Mode="ro" Description="Account list and per-account overrides. Must exist before first start. Template at https://github.com/marcelverdult/imap-spamfilter/blob/main/accounts.yml.example" Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/spamfilter/accounts.yml</Config>
  <Config Name="State dir" Target="/state" Default="/mnt/user/appdata/spamfilter/state" Mode="rw" Description="SQLite audit DB and heartbeat file. Persists across restarts." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/spamfilter/state</Config>

  <Config Name="RSPAMD_SCAN_URL" Target="RSPAMD_SCAN_URL" Default="http://spamfilter-rspamd:11333/checkv2" Mode="" Description="rspamd /checkv2 endpoint. Default works with the companion container on the same spamnet network." Type="Variable" Display="always" Required="true" Mask="false">http://spamfilter-rspamd:11333/checkv2</Config>
  <Config Name="RSPAMD_LEARN_URL" Target="RSPAMD_LEARN_URL" Default="http://spamfilter-rspamd:11334" Mode="" Description="rspamd controller base URL. Used for /learnspam and /learnham." Type="Variable" Display="always" Required="true" Mask="false">http://spamfilter-rspamd:11334</Config>
  <Config Name="RSPAMD_PASSWORD (override)" Target="RSPAMD_PASSWORD" Default="" Mode="" Description="Leave blank to use the auto-generated value from /state/controller.password (written by bootstrap.sh). Only set this if you want to override." Type="Variable" Display="advanced" Required="false" Mask="true"/>

  <Config Name="DEFAULT_JUNK_RETENTION_DAYS" Target="DEFAULT_JUNK_RETENTION_DAYS" Default="10" Mode="" Description="How many days a message stays in Junk before being MOVED to Trash. 0 disables. Used only when accounts.yml defaults does not set junk_retention_days." Type="Variable" Display="always" Required="false" Mask="false">10</Config>
  <Config Name="DEFAULT_TRAINED_RETENTION_DAYS" Target="DEFAULT_TRAINED_RETENTION_DAYS" Default="7" Mode="" Description="How many days a message stays in Trained-* before being MOVED to Trash. 0 disables. Used only when accounts.yml defaults does not set trained_retention_days." Type="Variable" Display="always" Required="false" Mask="false">7</Config>

  <Config Name="LOG_LEVEL" Target="LOG_LEVEL" Default="INFO" Mode="" Description="DEBUG | INFO | WARNING | ERROR" Type="Variable" Display="always" Required="false" Mask="false">INFO</Config>
  <Config Name="TZ" Target="TZ" Default="Europe/Berlin" Mode="" Description="Timezone for log timestamps" Type="Variable" Display="advanced" Required="false" Mask="false">Europe/Berlin</Config>
  <Config Name="STATE_DIR" Target="STATE_DIR" Default="/state" Mode="" Description="In-container path (matches the State dir mount)" Type="Variable" Display="advanced" Required="false" Mask="false">/state</Config>

  <Config Name="Dashboard port" Target="8080" Default="8080" Mode="tcp" Description="Host port for the optional read-only web dashboard. The container always listens on 8080 internally; this maps it to a host port of your choice. The dashboard starts once at least one login user exists. Add a user by running the helper inside the container: docker exec -it spamfilter python dashboard.py - it writes state/dashboard_users. No restart needed; the dashboard re-reads that file on each login." Type="Port" Display="always" Required="false" Mask="false">8080</Config>
  <Config Name="DASHBOARD_USERS" Target="DASHBOARD_USERS" Default="" Mode="" Description="Optional. Alternative to the state/dashboard_users file (preferred - run docker exec -it spamfilter python dashboard.py). Comma-separated username:hash:scope entries; scope is 'admin' or pipe-separated account names; merges with the file." Type="Variable" Display="advanced" Required="false" Mask="true"/>
  <Config Name="DASHBOARD_USER (legacy)" Target="DASHBOARD_USER" Default="" Mode="" Description="Legacy single-user login username. Prefer the state/dashboard_users file. Setting this and DASHBOARD_PASSWORD enables the dashboard with one plaintext account." Type="Variable" Display="advanced" Required="false" Mask="false"/>
  <Config Name="DASHBOARD_PASSWORD (legacy)" Target="DASHBOARD_PASSWORD" Default="" Mode="" Description="Legacy single-user login password, paired with DASHBOARD_USER. Prefer the state/dashboard_users file." Type="Variable" Display="advanced" Required="false" Mask="true"/>
</Container>
