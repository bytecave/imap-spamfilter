# Local runtime / secrets
.env
accounts.yml
rspamd/local.d/worker-controller.inc
rspamd/local.d/redis.conf

# Per-host data (Unraid appdata)
state/
# redis/ holds the appdata data dir and the rendered server config;
# the checked-in server config template must stay tracked.
redis/*
!redis/redis.conf.template
rspamd/data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Editor / OS
.idea/
.vscode/
.DS_Store
